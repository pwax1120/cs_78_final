pip install gdown is a dependency

Leave one model from a specific seed so that testing can be run on them.

I shouldn't need to add the datasets because it should have downloadable code. 

- remove the jsons
- add models back
- rename setup.sh to something else it sets up the SimCLR architecture
- might be easier to just run a test file with a demo .01 training with the whole thing instead of explaining it in the readme
- need to create a requirements.txt
- remove implementation
